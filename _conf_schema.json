{
  "provider_id": {
    "type": "string",
    "description": "分析模型 Provider ID",
    "_special": "select_provider",
    "hint": "用于每个 diff 分片分析和 JSON 修复。留空使用 AstrBot 当前默认模型。",
    "default": ""
  },
  "summary_provider_id": {
    "type": "string",
    "description": "总结模型 Provider ID",
    "_special": "select_provider",
    "hint": "用于总计全部的大模型分析内容。留空时使用分析模型；分析模型也留空时使用 AstrBot 当前默认模型。",
    "default": ""
  },
  "backup_provider_id_1": {
    "type": "string",
    "description": "备用模型 Provider ID 1",
    "_special": "select_provider",
    "hint": "主模型请求失败时按顺序尝试的备用模型。留空表示不启用。",
    "default": ""
  },
  "backup_provider_id_2": {
    "type": "string",
    "description": "备用模型 Provider ID 2",
    "_special": "select_provider",
    "hint": "备用模型 1 也失败时继续尝试。留空表示不启用。",
    "default": ""
  },
  "timeout_seconds": {
    "type": "int",
    "description": "大模型分析最大超时时间",
    "hint": "单位：秒。单次模型请求超过这个时间会视为失败。",
    "default": 120
  },
  "model_concurrency": {
    "type": "int",
    "description": "模型请求并发数",
    "hint": "允许同时进行的模型分析请求数量。默认 1 表示串行；大于 1 时会并发请求，但最终报告仍按文件分片顺序合并。",
    "default": 1
  },
  "target_groups": {
    "type": "list",
    "description": "推送群聊列表",
    "hint": "每项填写一个群号或 unified_msg_origin。填写纯群号时会通过 OneBot send_group_msg 发送；也可在目标群发送 /wtup_bind 获取当前群的 unified_msg_origin。",
    "default": [],
    "items": {
      "type": "string"
    }
  },
  "admin_targets": {
    "type": "list",
    "description": "管理员列表",
    "hint": "分析失败时私发通知这些管理员。每项填写一个私聊 unified_msg_origin 或 QQ 号；填写纯 QQ 号时会通过 OneBot send_private_msg 发送。",
    "default": [],
    "items": {
      "type": "string"
    }
  },
  "analysis_file_groups": {
    "type": "list",
    "description": "发送分析文件的群列表",
    "hint": "分析推送完成后，把本次 log 文件发送到这些群。每项填写一个群号或 unified_msg_origin。",
    "default": [],
    "items": {
      "type": "string"
    }
  },
  "monitor_interval_minutes": {
    "type": "int",
    "description": "监控频率",
    "hint": "单位：分钟。默认 30 分钟检查一次 gszabi99/War-Thunder-Datamine 的 master commit。",
    "default": 30
  },
  "analysis_prompt": {
    "type": "text",
    "description": "分析提示词",
    "hint": "用于指导分析模型如何分析 War Thunder Datamine 的 commit diff。",
    "default": "请分析 War Thunder Datamine 的 GitHub commit 更新内容，参考 War Thunder Datamine 更新日志格式，先整理本次更新条目，再给出 AI 分析。全程使用中文；载具若同时有英文名和中文名，写作 英文名(中文名)，如载具名称有特殊字符也要保留。请注意改动可能并非全部游戏模式，此为游戏《战争雷霆》的拆包文件，请你语言风格符合战争雷霆玩家。必须遵守后续系统给出的 JSON 输出格式要求。"
  },
  "summary_prompt": {
    "type": "text",
    "description": "总结提示词",
    "hint": "用于指导总结模型整理全部分片分析结果。留空时使用默认总结提示词。",
    "default": "请分析 War Thunder Datamine 的 GitHub commit 更新内容，参考 War Thunder Datamine 更新日志格式，先整理本次更新条目，再给出 AI 分析。全程使用中文；载具若同时有英文名和中文名，写作 英文名(中文名) 如载具名称有特殊字符要保留。请注意改动可能并非全部游戏模式，此为游戏《战争雷霆》的拆包文件，请你语言风格符合战争雷霆玩家。必须遵守后续系统给出的 JSON 输出格式要求。"
  },
  "enable_summary_model": {
    "type": "bool",
    "description": "是否启动总结模型",
    "hint": "开启后，当 diff 被拆成多次模型请求时，会先合并各分片分析结果，再调用总结模型整理最终报告。关闭时使用程序内置规则直接合并。不影响 JSON 修复和失败拆分重试。",
    "default": false
  },
  "enable_pre_summary_report": {
    "type": "bool",
    "description": "是否生成分析前报告",
    "hint": "开启后且“是否启动总结模型”也开启时，本次检查会生成两份报告：一份是总分析模型分析前的程序合并报告，一份是总分析模型分析后的最终报告。保存、/wtup_check 强制当前群发送、群推送和分析文件发送都会发送双份。",
    "default": false
  },
  "enable_push_append_text": {
    "type": "bool",
    "description": "推送时是否启动追加文字内容推送",
    "hint": "开启后，报告图片推送完成后再发送一条文字内容；/wtup_check 强制 会发送到当前群。启用双报告时，每份报告图片后都会追加一条对应文字。",
    "default": false
  },
  "push_append_text_template": {
    "type": "text",
    "description": "追加文字内容",
    "hint": "报告图片推送后追加发送的文字模板。可用变量：{version_range}=版本范围，例如 2.56.0.38->2.56.0.39；{token_count}=对应报告模型调用累计 total_tokens，双报告时分析前为总结模型调用前 token、分析后为最终 token；{elapsed_minutes}=从检查开始到报告生成的耗时分钟数，向上取整；{analysis_model}=分析模型完整 Provider ID；{summary_model}=总结模型完整 Provider ID，未启用或分析前报告时为“未启动”；{analysis_model_name}=分析模型纯模型名，会取最后一个 / 后面的内容，例如 NewAPI-OpenAI/glm-5.1 显示 glm-5.1；{summary_model_name}=总结模型纯模型名。",
    "default": "{version_range} 分析完成\n消耗token:{token_count}\n耗时{elapsed_minutes}分钟\n分析模型:{analysis_model}\n总结模型:{summary_model}"
  },
  "footer_note": {
    "type": "text",
    "description": "图片左下角文本",
    "hint": "显示在报告图片左下角，支持多行和简单 Markdown 链接，例如 [文字](https://example.com)。",
    "default": "[gszabi99/War-Thunder-Datamine](https://github.com/gszabi99/War-Thunder-Datamine)"
  },
  "github_token": {
    "type": "string",
    "description": "GitHub Personal Access Token",
    "hint": "可选。用于提高 GitHub API 访问限额。获取位置：https://github.com/settings/tokens 。公开仓库只读访问即可：Fine-grained token 选择 Public repositories read-only；Classic token 可不勾选 scopes。留空则匿名访问，限额较低。",
    "default": ""
  },
  "max_files_per_report": {
    "type": "int",
    "description": "每次模型请求最多文件数",
    "hint": "默认 0 表示不限制。若设置为大于 0，会先按文件数拆出基础模型请求，且每个请求不会超过该文件数上限；单个文件不会被拆开。",
    "default": 0
  },
  "max_input_tokens": {
    "type": "float",
    "description": "每次模型请求最大 token 输入",
    "hint": "默认 0 表示不限制。支持最多两位小数，例如 0.25K 或 1.5M。文件数限制先应用，再按完整模型输入估算 token；超过后会在保证文件完整性的前提下继续拆分任务。",
    "default": 0
  },
  "max_input_token_unit": {
    "type": "string",
    "description": "token 输入单位",
    "hint": "K 表示千 token，M 表示百万 token。",
    "options": ["K", "M"],
    "default": "K"
  },
  "max_retry_count": {
    "type": "int",
    "description": "最大重试次数",
    "hint": "模型请求失败后，每次把上次失败任务拆成两半再重试；只按文件边界拆分，不损坏文件完整性。默认 2。",
    "default": 2
  },
  "max_saved_artifacts": {
    "type": "int",
    "description": "插件文件最大保存数量",
    "hint": "分别限制 logs、images、errors 目录中保留的文件数量。默认 5，超过后删除旧文件并保留最新文件；设置为 0 表示不限制。",
    "default": 5
  }
}
