{
  "provider_id": {
    "type": "string",
    "description": "模型 Provider ID",
    "_special": "select_provider",
    "hint": "留空使用 AstrBot 当前默认模型；填写 AstrBot 后台中模型提供商的 Provider ID。",
    "default": ""
  },
  "timeout_seconds": {
    "type": "int",
    "description": "大模型分析最大超时时间",
    "hint": "单位：秒。单次模型请求超过这个时间会视为失败。",
    "default": 120
  },
  "model_concurrency": {
    "type": "int",
    "description": "模型请求并发数",
    "hint": "允许同时进行的模型分析请求数量。默认 1 表示串行；大于 1 时会并发请求，但最终报告仍按文件分片顺序合并。",
    "default": 1
  },
  "target_groups": {
    "type": "list",
    "description": "推送群聊列表",
    "hint": "每项填写一个群号或 unified_msg_origin。填写纯群号时会通过 OneBot send_group_msg 发送；也可在目标群发送 /wtup_bind 获取当前群的 unified_msg_origin。",
    "default": [],
    "items": {
      "type": "string"
    }
  },
  "monitor_interval_minutes": {
    "type": "int",
    "description": "监控频率",
    "hint": "单位：分钟。默认 30 分钟检查一次 gszabi99/War-Thunder-Datamine 的 master commit。",
    "default": 30
  },
  "analysis_prompt": {
    "type": "text",
    "description": "分析提示词",
    "hint": "用于指导模型如何分析 War Thunder Datamine 的 commit diff。",
    "default": "请分析 War Thunder Datamine 的 GitHub commit 更新内容，参考 War Thunder Datamine 更新日志格式，先整理本次更新条目，再给出 AI 分析。全程使用中文；载具若同时有英文名和中文名，写作 英文名(中文名)，如载具名称有特殊字符也要保留。请注意改动可能并非全部游戏模式，此为游戏《战争雷霆》的拆包文件，请你语言风格符合战争雷霆玩家。必须遵守后续系统给出的 JSON 输出格式要求。"
  },
  "enable_second_pass_analysis": {
    "type": "bool",
    "description": "是否启用二次分析",
    "hint": "开启后，当 diff 被拆成多次模型请求时，会先合并各分片分析结果，再额外请求一次模型整理最终报告。关闭时使用程序内置规则直接合并。",
    "default": false
  },
  "github_token": {
    "type": "string",
    "description": "GitHub Personal Access Token",
    "hint": "可选。用于提高 GitHub API 访问限额。获取位置：https://github.com/settings/tokens 。公开仓库只读访问即可：Fine-grained token 选择 Public repositories read-only；Classic token 可不勾选 scopes。留空则匿名访问，限额较低。",
    "default": ""
  },
  "max_files_per_report": {
    "type": "int",
    "description": "每次模型请求最多文件数",
    "hint": "默认 0 表示不限制。若设置为大于 0，超过后会拆成多次模型请求，最终合并为一份报告并只推送一次。",
    "default": 0
  },
  "max_patch_chars": {
    "type": "int",
    "description": "每次模型请求最大 diff 字符数",
    "hint": "默认 0 表示不限制。若设置为大于 0，超过后会拆成多次模型请求，最终合并为一份报告并只推送一次。",
    "default": 0
  }
}
