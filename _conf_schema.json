{
  "provider_id": {
    "type": "string",
    "description": "分析模型 Provider ID",
    "_special": "select_provider",
    "hint": "用于每个 diff 分片分析和 JSON 修复。留空使用 AstrBot 当前默认模型。",
    "default": ""
  },
  "summary_provider_id": {
    "type": "string",
    "description": "总结模型 Provider ID",
    "_special": "select_provider",
    "hint": "用于总计全部的大模型分析内容。留空时使用分析模型；分析模型也留空时使用 AstrBot 当前默认模型。",
    "default": ""
  },
  "backup_provider_id_1": {
    "type": "string",
    "description": "备用模型 Provider ID 1",
    "_special": "select_provider",
    "hint": "主模型请求失败时按顺序尝试的备用模型。留空表示不启用。",
    "default": ""
  },
  "backup_provider_id_2": {
    "type": "string",
    "description": "备用模型 Provider ID 2",
    "_special": "select_provider",
    "hint": "备用模型 1 也失败时继续尝试。留空表示不启用。",
    "default": ""
  },
  "timeout_seconds": {
    "type": "int",
    "description": "大模型分析最大超时时间",
    "hint": "单位：秒。单次模型请求超过这个时间会视为失败。",
    "default": 120
  },
  "model_concurrency": {
    "type": "int",
    "description": "模型请求并发数",
    "hint": "允许同时进行的模型分析请求数量。默认 1 表示串行；大于 1 时会并发请求，但最终报告仍按文件分片顺序合并。",
    "default": 1
  },
  "enable_streaming_llm_call": {
    "type": "bool",
    "description": "启用流式模型请求",
    "hint": "默认关闭，使用原有非流式调用方式。开启后，插件会优先使用 AstrBot Provider 的 text_chat_stream 流式接口并聚合结果，适用于仅支持流式请求的服务。",
    "default": false
  },
  "target_groups": {
    "type": "list",
    "description": "推送群聊列表",
    "hint": "每项填写一个群号或 unified_msg_origin。填写纯群号时会通过 OneBot send_group_msg 发送；也可在目标群发送 /wtup_bind 获取当前群的 unified_msg_origin。",
    "default": [],
    "items": {
      "type": "string"
    }
  },
  "admin_targets": {
    "type": "list",
    "description": "管理员列表",
    "hint": "仅这些管理员可执行 /wtup_check 强制 和 /wtup_check 强制全部；分析失败时也会私发通知这些管理员。每项填写一个私聊 unified_msg_origin 或 QQ 号；填写纯 QQ 号时会通过 OneBot send_private_msg 发送。",
    "default": [],
    "items": {
      "type": "string"
    }
  },
  "analysis_file_groups": {
    "type": "list",
    "description": "发送分析文件的群列表",
    "hint": "分析推送完成后，把本次 log 文件发送到这些群。每项填写一个群号或 unified_msg_origin。",
    "default": [],
    "items": {
      "type": "string"
    }
  },
  "monitor_interval_minutes": {
    "type": "int",
    "description": "监控频率",
    "hint": "单位：分钟。默认 30 分钟检查一次 gszabi99/War-Thunder-Datamine 的 master commit。",
    "default": 30
  },
  "analysis_prompt": {
    "type": "text",
    "description": "分析提示词",
    "hint": "用于指导分析模型如何分析 War Thunder Datamine 的 commit diff。系统会额外强制要求模型输出 analysis_coverage 覆盖清单，逐文件标记已分析内容、证据和待复核原因。",
    "default": "请分析 War Thunder Datamine 的 GitHub commit 更新内容。你是分片分析模型，只负责当前输入中可见的 diff 内容；最终报告会由程序和总结模型合并。\n\n核心目标：完整、准确、可核对。必须逐文件、逐条分析当前输入中的全部可见改动，不得因为改动很小、看似内部调整、纯文本、本地化、重命名、新增/删除文件、格式变化或数值微调而跳过。\n\n更新条目要求：\n1. 按 War Thunder Datamine 更新日志风格整理，先列“更新条目”，再给出 AI 分析。\n2. 每条改动必须保留关键细节：对象名称、参数名、旧值/新值、新增/删除项、文本键、文件语义或机制变化。\n3. 涉及参数时，参数名后要用圆括号补充中文含义，例如 uncageBeforeLaunch (发射前解除陀螺锁定)。无法可靠判断含义时写“不确定”，不要编造。\n4. 涉及载具、武器、弹药、挂载、改装件、乘员组或装备时，必须尽量写出完整名称；能判断中英文时使用 英文原名(中文译名)。禁止用“部分载具”“某些装备”代替明确对象。\n5. 每条改动都要说明可能影响：会怎样影响游戏内表现、手感、平衡、经济、任务、UI 或玩家理解。若没有明显直接影响，明确写“对游戏表现无明显直接影响”。\n\n分类要求：\n1. 使用清晰中文分类，例如“空中载具”“地面载具”“直升机”“武器/弹药”“经济/科技树”“地图/任务”“文本/UI”“其他变化”“水面舰艇”。\n2. 无法明确归类的内容放入“其他变化”。\n3. 水面舰艇相关内容放在所有分类最后；没有内容的分类省略。\n\n新覆盖清单要求：\n1. 必须配合插件后续 JSON 协议输出 analysis_coverage。\n2. analysis_coverage 是防遗漏清单，不是正文；当前输入中的每个文件都必须有独立记录，path 必须与输入文件名完全一致。\n3. 只有确认该文件所有可见改动都已经写入 update_sections 或 ai_analysis，才能标记 analyzed。\n4. 如果只分析了一部分、上下文不足、影响不明确或工具结果不足，标记 uncertain，并在 notes 写明原因。\n5. covered_changes 必须列出该文件已经覆盖的改动点；evidence 必须写能回指 diff 的参数名、实体名、文本键、文件片段或数值变化，不能只写“已检查”。\n\n语言与边界：\n1. 全程中文，信息优先，语气可以有《战争雷霆》玩家熟悉的轻微吐槽，但不要牺牲清晰度。\n2. 不要输出 Markdown 代码块，不要输出 JSON 之外的解释。\n3. 不要在最终内容里写“当前分片”“本批 diff”“第几批”等内部拆分语境；统一说“本次 diff”。\n4. 不确定的信息写入 uncertainties，不要猜测、不要补充输入中没有的事实。"
  },
  "summary_prompt": {
    "type": "text",
    "description": "总结提示词",
    "hint": "用于指导总结模型整理全部分片分析结果。留空时使用默认总结提示词。系统会额外要求总结模型保留 analysis_coverage 覆盖清单，不得删除或压缩已标记的文件路径。",
    "default": "你是最终整理模型，只负责基于已经给出的分片分析 JSON 或程序初步合并 JSON 做去重、合并和排版整理。不要重新分析原始 diff，不要新增输入中没有的事实，不要扩大解释。\n\n你的任务只有这些：\n1. 合并重复条目：删除完全重复内容，合并同一对象、同一参数或同一机制的近似表述。\n2. 保留独立改动：不同文件、不同对象、不同参数、不同数值、不同文本键或不同影响的内容不能因为看起来相似就删掉。\n3. 整理分类顺序：使用清晰中文分类；无法归类放入“其他变化”；水面舰艇相关内容放在最后；空分类省略。\n4. 压缩冗余表达：让正文更适合最终推送，减少重复句、分片语境和过长说明，但必须保留关键细节、数值变化、对象名称和影响判断。\n5. 整理 AI 分析：changed_content、player_impact、uncertainties 只做归纳去重，不重新发明结论；已有不确定点和分析失败信息必须保留。\n\nanalysis_coverage 硬性要求：\n1. 必须完整保留输入中已有的 analysis_coverage，不能删除、合并、改名或压缩任何 path。\n2. 不要把多个文件合成一个 coverage 项；每个 path 仍然独立存在。\n3. 不要把 uncertain/skipped 擅自改成 analyzed；除非输入中已经明确证明该文件所有可见改动都被覆盖。\n4. covered_changes、evidence、notes 中已有的信息要保留；可以去重和轻微润色，但不能抹掉证据或复核原因。\n5. 如果发现 coverage 中某个文件的独立改动没有出现在正文，应优先把该改动补回 update_sections，而不是删除 coverage。\n\n输出风格：\n1. 全程中文，只输出插件要求的 JSON，不要输出 Markdown 代码块、前言或解释。\n2. 最终报告要像一次完整更新，不要出现“分片”“本批”“当前批次”“Part”等内部流程词。\n3. 如果输入信息不足，只保留“不确定/需复核”的说明；不要编造载具名称、参数含义、游戏影响或版本结论。"
  },
  "enable_summary_model": {
    "type": "bool",
    "description": "是否启动总结模型",
    "hint": "开启后，当 diff 被拆成多次模型请求时，会先合并各分片分析结果，再调用总结模型整理最终报告。关闭时使用程序内置规则直接合并。不影响 JSON 修复和失败拆分重试。",
    "default": false
  },
  "enable_pre_summary_report": {
    "type": "bool",
    "description": "是否生成分析前报告",
    "hint": "开启后且“是否启动总结模型”也开启时，本次检查会生成两份报告：一份是总分析模型分析前的程序合并报告，一份是总分析模型分析后的最终报告。保存、/wtup_check 强制当前群发送、群推送和分析文件发送都会发送双份。",
    "default": false
  },
  "enable_push_append_text": {
    "type": "bool",
    "description": "推送时是否启动追加文字内容推送",
    "hint": "开启后，报告图片推送完成后再发送一条文字内容；/wtup_check 强制 会发送到当前群。启用双报告时，每份报告图片后都会追加一条对应文字。",
    "default": false
  },
  "push_append_text_template": {
    "type": "text",
    "description": "追加文字内容",
    "hint": "报告图片推送后追加发送的文字模板。可用变量：{version_range}=版本范围，例如 2.56.0.38->2.56.0.39；{token_count}=对应报告模型调用累计 total_tokens，双报告时分析前为总结模型调用前 token、分析后为最终 token；{elapsed_duration} 或 {耗时}=从检查开始到报告生成的耗时，格式为 x分x秒；{elapsed_minutes}=耗时分钟数，向上取整；{analysis_model}=分析模型完整 Provider ID；{summary_model}=总结模型完整 Provider ID，未启用或分析前报告时为“未启动”；{analysis_model_name}=分析模型纯模型名，会取最后一个 / 后面的内容，例如 NewAPI-OpenAI/glm-5.1 显示 glm-5.1；{summary_model_name}=总结模型纯模型名。",
    "default": "{version_range} 分析\n消耗token:{token_count}  耗时: {耗时}\n分析模型:{analysis_model_name}\n总结模型:{summary_model_name}"
  },
  "footer_note": {
    "type": "text",
    "description": "图片左下角文本",
    "hint": "显示在报告图片左下角，支持多行和简单 Markdown 链接，例如 [文字](https://example.com)。",
    "default": "[gszabi99/War-Thunder-Datamine](https://github.com/gszabi99/War-Thunder-Datamine)"
  },
  "github_token": {
    "type": "string",
    "description": "GitHub Personal Access Token",
    "hint": "可选。用于提高 GitHub API 访问限额。获取位置：https://github.com/settings/tokens 。公开仓库只读访问即可：Fine-grained token 选择 Public repositories read-only；Classic token 可不勾选 scopes。留空则匿名访问，限额较低。",
    "default": ""
  },
  "max_files_per_report": {
    "type": "int",
    "description": "每次模型请求最多文件数",
    "hint": "默认 150。设置为 0 表示不限制。若设置为大于 0，会先按文件数拆出基础模型请求，且每个请求不会超过该文件数上限；单个文件不会被拆开。",
    "default": 150
  },
  "max_input_tokens": {
    "type": "float",
    "description": "每次模型请求最大 token 输入",
    "hint": "默认 0.1M。设置为 0 表示不限制。支持最多两位小数，例如 0.25K 或 1.5M。文件数限制先应用，再按完整模型输入估算 token；超过后会在保证文件完整性的前提下继续拆分任务。",
    "default": 0.1
  },
  "max_input_token_unit": {
    "type": "string",
    "description": "token 输入单位",
    "hint": "K 表示千 token，M 表示百万 token。",
    "options": ["K", "M"],
    "default": "M"
  },
  "max_retry_count": {
    "type": "int",
    "description": "最大重试次数",
    "hint": "模型请求失败后，每次把上次失败任务拆成两半再重试；只按文件边界拆分，不损坏文件完整性。默认 2。",
    "default": 2
  },
  "enable_model_tool_calls": {
    "type": "bool",
    "description": "允许模型请求补充上下文",
    "hint": "开启后，分析模型可以在 JSON 的 tool_calls 中申请读取本次 diff 的 patch、读取目标提交文件全文、搜索变更文件或列出关联文件。read_changed_file 本地没有完整文件内容时允许额外从 GitHub 拉取目标提交下的文件。",
    "default": false
  },
  "max_tool_call_rounds": {
    "type": "int",
    "description": "每个分片最多工具调用轮数",
    "hint": "每个分片分析后，模型最多可申请多少轮补充上下文。设置为 0 等同关闭工具补充分析。",
    "default": 2
  },
  "max_tool_calls_per_round": {
    "type": "int",
    "description": "每轮最多工具调用数",
    "hint": "限制模型每轮最多申请多少个工具调用，超出的请求会被拒绝并写入任务日志。",
    "default": 5
  },
  "max_tool_result_chars": {
    "type": "int",
    "description": "单次工具结果最大字符数",
    "hint": "限制每个工具返回给模型的最大字符数，避免补充上下文过大。默认 12000。",
    "default": 12000
  },
  "tool_call_prompt": {
    "type": "text",
    "description": "工具调用提示词",
    "hint": "用于指导模型什么时候可以申请补充上下文。插件会追加固定 JSON 协议说明；这里填写业务侧偏好和限制。",
    "default": "当当前分片不足以判断关联挂载、完整参数、同名配置、实体归属、参数含义，或 analysis_coverage 缺少可回指 diff 的证据时，可以通过 tool_calls 申请补充上下文。只请求本次 diff 涉及或强相关的文件；优先使用 read_changed_patch 查看变更片段，必要时用 read_changed_file 读取目标提交文件全文，用 search_changed_files 搜索参数名/实体名/文本键，用 list_related_files 查找同名或强关联文件。不要请求无关路径，不要为了普通概括请求工具；如果工具结果仍不足，必须在 uncertainties 和 analysis_coverage.notes 中标记待复核。"
  },
  "clear_cache_files": {
    "type": "bool",
    "description": "清空缓存文件",
    "hint": "默认关闭。开启后插件下次加载时会清空插件数据目录中的 GitHub 缓存、模型分析缓存、日志、图片和状态文件，然后自动把此开关改回关闭。",
    "default": false
  },
  "max_saved_artifacts": {
    "type": "int",
    "description": "插件文件最大保存数量",
    "hint": "logs 目录按任务结果目录保留数量，images、errors、task_logs 目录按文件保留数量。默认 5，超过后删除旧项目并保留最新项目；设置为 0 表示不限制。GitHub 缓存目录 github_cache 和模型分析缓存目录 analysis_cache 不受此项限制。",
    "default": 5
  }
}
