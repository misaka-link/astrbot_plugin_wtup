{
  "provider_id": {
    "type": "string",
    "description": "分析模型 Provider ID",
    "_special": "select_provider",
    "hint": "用于每个 diff 分片分析和 JSON 修复。留空使用 AstrBot 当前默认模型。",
    "default": ""
  },
  "summary_provider_id": {
    "type": "string",
    "description": "总结模型 Provider ID",
    "_special": "select_provider",
    "hint": "用于总计全部的大模型分析内容。留空时使用分析模型；分析模型也留空时使用 AstrBot 当前默认模型。",
    "default": ""
  },
  "review_provider_id": {
    "type": "string",
    "description": "节能监督模型 Provider ID",
    "_special": "select_provider",
    "hint": "节能档/自动档普通复核使用。留空使用分析模型。建议使用便宜快速模型。",
    "default": ""
  },
  "review_quality_provider_id": {
    "type": "string",
    "description": "质量监督模型 Provider ID",
    "_special": "select_provider",
    "hint": "质量档或自动升级质量档时使用。留空时优先使用节能监督模型，再留空使用分析模型。",
    "default": ""
  },
  "backup_provider_id_1": {
    "type": "string",
    "description": "备用模型 Provider ID 1",
    "_special": "select_provider",
    "hint": "主模型请求失败时按顺序尝试的备用模型。留空表示不启用。",
    "default": ""
  },
  "backup_provider_id_2": {
    "type": "string",
    "description": "备用模型 Provider ID 2",
    "_special": "select_provider",
    "hint": "备用模型 1 也失败时继续尝试。留空表示不启用。",
    "default": ""
  },
  "timeout_seconds": {
    "type": "int",
    "description": "大模型分析最大超时时间",
    "hint": "单位：秒。单次模型请求超过这个时间会视为失败。",
    "default": 120
  },
  "model_concurrency": {
    "type": "int",
    "description": "模型请求并发数",
    "hint": "允许同时进行的模型分析请求数量。默认 1 表示串行；大于 1 时会并发请求，但最终报告仍按文件分片顺序合并。",
    "default": 1
  },
  "enable_streaming_llm_call": {
    "type": "bool",
    "description": "启用流式模型请求",
    "hint": "默认关闭，使用原有非流式调用方式。开启后，插件会优先使用 AstrBot Provider 的 text_chat_stream 流式接口并聚合结果，适用于仅支持流式请求的服务。",
    "default": false
  },
  "target_groups": {
    "type": "list",
    "description": "推送群聊列表",
    "hint": "每项填写一个群号或 unified_msg_origin。填写纯群号时会通过 OneBot send_group_msg 发送；也可在目标群发送 /wtup_bind 获取当前群的 unified_msg_origin。",
    "default": [],
    "items": {
      "type": "string"
    }
  },
  "admin_targets": {
    "type": "list",
    "description": "管理员列表",
    "hint": "仅这些管理员可执行 /wtup_check 强制 和 /wtup_check 强制全部；分析失败时也会私发通知这些管理员。每项填写一个私聊 unified_msg_origin 或 QQ 号；填写纯 QQ 号时会通过 OneBot send_private_msg 发送。",
    "default": [],
    "items": {
      "type": "string"
    }
  },
  "analysis_file_groups": {
    "type": "list",
    "description": "发送分析文件的群列表",
    "hint": "分析推送完成后，把本次 log 文件发送到这些群。每项填写一个群号或 unified_msg_origin。",
    "default": [],
    "items": {
      "type": "string"
    }
  },
  "monitor_interval_minutes": {
    "type": "int",
    "description": "监控频率",
    "hint": "单位：分钟。默认 30 分钟检查一次 gszabi99/War-Thunder-Datamine 的 master commit。",
    "default": 30
  },
  "analysis_prompt": {
    "type": "text",
    "description": "分析提示词",
    "hint": "用于指导分析模型如何分析 War Thunder Datamine 的 commit diff，并把批量/重复/需验证内容分流到报告下方。",
    "default": "请分析 War Thunder Datamine 的 GitHub commit 更新内容，参考 War Thunder Datamine 更新日志的格式，先整理本次更新条目，再给出 AI 分析。全程使用中文，并严格遵循以下原则：\n全面覆盖所有改动：必须将 commit 中的全部变动逐一处理，包括但不限于载具参数、武器/弹药、装甲模型、损伤模型、飞行/机动模型、改装件、乘员组技能、经济与研发、科技树、地图/任务、音效、贴图、UI 文本、本地化字符串以及任何 XML/JSON/配置文件中的数值或逻辑修改。不得遗漏任何一条变更，即使只是校对性文本改动或极小的数值微调。\n载具与装备精确点名：每一项改动如果涉及载具（飞机、坦克、舰艇、直升机）、武器、弹药、改装件、乘员组或其他装备，都必须写出其完整名称，采用 英文原名(中文译名) 的格式（例如 M1 Abrams(M1 艾布拉姆斯)）。若名称中含有特殊字符（如括号、引号、小数点等），必须原样保留，不得修改或删除。禁止使用“部分载具”“某些装备”等模糊表述，必须逐一列出具体载具和具体装备名称。\n批量重复内容分流：遇到大规模同类机械修改、重复或高度相似内容、批量但含义不明确需要验证的内容时，不要塞进主要更新条目。必须从 update_sections 中剔出，放入 bulk_repeat_content：batch 写批量修改，repeated 写重复内容，needs_verification 写批量/重复但含义不明确或需要实测、官方确认、人工验证的内容。bulk_repeat_content 会显示在报告更新内容下方、AI 分析上方；这里的每条内容也必须保留 source_ids。不得把同一批重复内容在 update_sections 中再次铺开。\n杜绝丢弃内容：在“更新条目”部分保留实际有信息量、可解释的改动；批量重复内容按上一条分流。对于代码/配置中的参数名称，尽量在其后紧跟圆括号补充中文说明，解释该参数的大致功能或含义（例如 uncageBeforeLaunch(发射前解除陀螺锁定)）。每条主要改动尽量用一两句话简述该改动对游戏内表现或手感的影响；若影响不明显或仅为内部调整，也需明确说明“对游戏表现无明显直接影响”。\n合理划分分类并固定顺序：将主要改动条目划分到合适的分类下，例如：“空中载具”“地面载具”“水面舰艇”“直升机”“武器/弹药”“地图/任务”“经济/科技树”“其他机制”等。如果出现无法明确归入上述分类的内容（如通用 UI、全局参数、脚本逻辑等），一律放入“其他”分类，并置于“水面舰艇”之前。特别注意：所有“水面舰艇”相关的内容必须排在最下方（即置于所有其他分类之后）。若某一分类无内容，则可省略。\n语言风格：整理更新条目时保持格式工整、信息准确。AI 分析部分则用《战争雷霆》玩家熟悉的语言风格来撰写（可以带有安东星式的幽默或吐槽，但不要影响信息的清晰传达）。请注意，改动内容可能并非适用于所有游戏模式（因为数据来源为游戏拆包文件）。\n输出格式：必须严格遵守后续系统给出的 JSON 输出格式要求，将完整的更新条目、批量重复内容和 AI 分析分别放入对应字段中。必须报告全部改动，划分好分类，不丢弃任何内容。"
  },
  "summary_prompt": {
    "type": "text",
    "description": "总结提示词",
    "hint": "用于指导总结模型整理全部分片分析结果，并保留批量/重复/需验证内容分流。留空时使用默认总结提示词。",
    "default": "请整理 War Thunder Datamine 的 GitHub commit 更新内容，参考 War Thunder Datamine 更新日志的格式，先整理本次更新条目，再给出 AI 分析。全程使用中文，并严格遵循以下原则：\n全面覆盖所有改动：必须将 commit 中的全部变动逐一处理，包括但不限于载具参数、武器/弹药、装甲模型、损伤模型、飞行/机动模型、改装件、乘员组技能、经济与研发、科技树、地图/任务、音效、贴图、UI 文本、本地化字符串以及任何 XML/JSON/配置文件中的数值或逻辑修改。不得遗漏任何一条变更，即使只是校对性文本改动或极小的数值微调。\n载具与装备精确点名：每一项改动如果涉及载具（飞机、坦克、舰艇、直升机）、武器、弹药、改装件、乘员组或其他装备，都必须写出其完整名称，采用 英文原名(中文译名) 的格式（例如 M1 Abrams(M1 艾布拉姆斯)）。若名称中含有特殊字符（如括号、引号、小数点等），必须原样保留，不得修改或删除。禁止使用“部分载具”“某些装备”等模糊表述，必须逐一列出具体载具和具体装备名称。\n批量重复内容分流：遇到大规模同类机械修改、重复或高度相似内容、批量但含义不明确需要验证的内容时，不要塞进主要更新条目。必须从 update_sections 中剔出，放入 bulk_repeat_content：batch 写批量修改，repeated 写重复内容，needs_verification 写批量/重复但含义不明确或需要实测、官方确认、人工验证的内容。bulk_repeat_content 会显示在报告更新内容下方、AI 分析上方；这里的每条内容也必须保留 source_ids。不得把同一批重复内容在 update_sections 中再次铺开。\n杜绝丢弃内容：在“更新条目”部分保留实际有信息量、可解释的改动；批量重复内容按上一条分流。对于代码/配置中的参数名称，尽量在其后紧跟圆括号补充中文说明，解释该参数的大致功能或含义（例如 uncageBeforeLaunch(发射前解除陀螺锁定)）。每条主要改动尽量用一两句话简述该改动对游戏内表现或手感的影响；若影响不明显或仅为内部调整，也需明确说明“对游戏表现无明显直接影响”。\n合理划分分类并固定顺序：将主要改动条目划分到合适的分类下，例如：“空中载具”“地面载具”“水面舰艇”“直升机”“武器/弹药”“地图/任务”“经济/科技树”“其他机制”等。如果出现无法明确归入上述分类的内容（如通用 UI、全局参数、脚本逻辑等），一律放入“其他”分类，并置于“水面舰艇”之前。特别注意：所有“水面舰艇”相关的内容必须排在最下方（即置于所有其他分类之后）。若某一分类无内容，则可省略。\n语言风格：整理更新条目时保持格式工整、信息准确。AI 分析部分则用《战争雷霆》玩家熟悉的语言风格来撰写（可以带有安东星式的幽默或吐槽，但不要影响信息的清晰传达）。请注意，改动内容可能并非适用于所有游戏模式（因为数据来源为游戏拆包文件）。\n输出格式：必须严格遵守后续系统给出的 JSON 输出格式要求，将完整的更新条目、批量重复内容和 AI 分析分别放入对应字段中。必须报告全部改动，划分好分类，不丢弃任何内容。"
  },
  "enable_summary_model": {
    "type": "bool",
    "description": "是否启动总结模型",
    "hint": "开启后，当 diff 被拆成多次模型请求时，会先合并各分片分析结果，再调用总结模型整理最终报告。关闭时使用程序内置规则直接合并。不影响 JSON 修复和失败拆分重试。",
    "default": false
  },
  "enable_review_model": {
    "type": "bool",
    "description": "启用监督模型复核",
    "hint": "默认关闭。开启后在最终报告生成前增加一次监督模型复核；程序仍会先做 source_ids 硬覆盖检查，监督模型主要负责质检。",
    "default": false
  },
  "review_mode": {
    "type": "string",
    "description": "监督模型预设",
    "hint": "off=关闭；energy=节能档，只查漏编号、格式、模糊表述和必要影响说明；quality=质量档，会更细地检查解释、分类和参数含义；auto=自动档，平时节能，发现大量漏项或补充失败时升级质量档。默认推荐 quality。",
    "options": [
      "off",
      "energy",
      "quality",
      "auto"
    ],
    "default": "quality"
  },
  "review_prompt": {
    "type": "text",
    "description": "监督模型提示词",
    "hint": "用于约束监督模型如何复核报告。节能档建议只查覆盖和格式；质量档再检查解释质量，并通过 item_revisions 返回局部文字修正。",
    "default": "你是 War Thunder Datamine 更新报告的监督/质检模型。请基于程序提供的点名册覆盖状态和结构化报告做复核，不要重新创作整篇报告。节能档只检查是否漏编号、JSON 结构、source_ids 保留、是否存在“部分载具/若干装备”等模糊表述、每条是否有必要的影响说明；质量档还需要检查分类是否合理、参数说明是否清楚、条目是否和对应变更说明匹配。如需修正文案，质量档只返回按 item_id 定位的 item_revisions，程序会保留原 source_ids 和结构。"
  },
  "review_rounds": {
    "type": "int",
    "description": "监督模型最大轮数",
    "hint": "默认 1。质量档可设置为 2 让监督模型修正后再复查；节能档通常 1 即可。",
    "default": 1
  },
  "review_energy_batch_size": {
    "type": "int",
    "description": "节能档复核条目数",
    "hint": "节能档最多抽取/携带多少条结构化报告条目给监督模型。默认 80，值越大 token 越多。",
    "default": 80
  },
  "review_quality_batch_size": {
    "type": "int",
    "description": "质量档复核条目数",
    "hint": "质量档最多携带多少条结构化报告条目给监督模型。默认 25，值越大越细但 token 越多。",
    "default": 25
  },
  "review_upgrade_missing_id_threshold": {
    "type": "int",
    "description": "自动升级质量档漏项阈值",
    "hint": "自动档下，程序补入的未覆盖编号数量超过该值时升级质量档。默认 10。",
    "default": 10
  },
  "review_upgrade_on_missing_ids": {
    "type": "bool",
    "description": "漏项时自动升级质量档",
    "hint": "默认开启。自动档下发现未覆盖编号超过阈值时，使用质量监督模型复核。",
    "default": true
  },
  "review_upgrade_on_context_failure": {
    "type": "bool",
    "description": "补充失败时自动升级质量档",
    "hint": "默认开启。自动档下检测到动态补充失败、无法读取、需复核未覆盖变更等风险时，使用质量监督模型复核。",
    "default": true
  },
  "enable_pre_summary_report": {
    "type": "bool",
    "description": "是否生成分析前报告",
    "hint": "开启后且“是否启动总结模型”也开启时，本次检查会生成两份报告：一份是总分析模型分析前的程序合并报告，一份是总分析模型分析后的最终报告。保存、/wtup_check 强制当前群发送、群推送和分析文件发送都会发送双份。",
    "default": false
  },
  "enable_push_append_text": {
    "type": "bool",
    "description": "推送时是否启动追加文字内容推送",
    "hint": "开启后，报告图片推送完成后再发送一条文字内容；/wtup_check 强制 会发送到当前群。启用双报告时，每份报告图片后都会追加一条对应文字。",
    "default": false
  },
  "render_mode": {
    "type": "string",
    "description": "报告渲染方式",
    "hint": "t2i：使用 AstrBot html_render/文转图服务，失败自动尝试 Playwright；playwright：使用本地 Chromium 截图，失败自动尝试 t2i；text：纯文字输出。图片模式下两种图片渲染均失败时，默认只发送降级提示，不发送整篇文字报告。",
    "options": [
      "t2i",
      "playwright",
      "text"
    ],
    "default": "t2i"
  },
  "enable_render_fallback_text": {
    "type": "bool",
    "description": "图片失败后发送文字报告",
    "hint": "默认关闭。仅在 render_mode 为 t2i 或 playwright 且两种图片渲染都失败时生效；开启后会发送“图片渲染失败，已自动切换为文字模式”并附带整篇纯文字报告。关闭时只发送失败提示，避免长文字刷屏。",
    "default": false
  },
  "push_append_text_template": {
    "type": "text",
    "description": "追加文字内容",
    "hint": "报告图片推送后追加发送的文字模板。可用变量：{version_range}=版本范围，例如 2.56.0.38->2.56.0.39；{token_count}=对应报告模型调用累计 total_tokens，双报告时分析前为总结模型调用前 token、分析后为最终 token；{elapsed_duration} 或 {耗时}=从检查开始到报告生成的耗时，格式为 x分x秒；{elapsed_minutes}=耗时分钟数，向上取整；{analysis_model}=分析模型实际 Provider ID 链路；{summary_model}=总结模型实际 Provider ID 链路，未启用或分析前报告时为“未启动”；{analysis_model_name}=分析模型纯模型名链路，会取最后一个 / 后面的内容，例如 NewAPI-OpenAI/glm-5.1 -> NewAPI-Gemini/gemini-3.5-flash 显示 glm-5.1 -> gemini-3.5-flash；{summary_model_name}=总结模型纯模型名链路；{analysis_model_chain}/{summary_model_chain}/{analysis_model_chain_name}/{summary_model_chain_name}=对应链路变量别名。",
    "default": "{version_range} 分析\n消耗token:{token_count}  耗时: {耗时}\n分析模型:{analysis_model_name}\n总结模型:{summary_model_name}"
  },
  "footer_note": {
    "type": "text",
    "description": "图片左下角文本",
    "hint": "显示在报告图片左下角，支持多行和简单 Markdown 链接，例如 [文字](https://example.com)。",
    "default": "[gszabi99/War-Thunder-Datamine](https://github.com/gszabi99/War-Thunder-Datamine)"
  },
  "watermark_text": {
    "type": "string",
    "description": "水印内容",
    "hint": "显示在报告图片中的平铺水印。留空则不增加水印。",
    "default": ""
  },
  "watermark_opacity_percent": {
    "type": "int",
    "description": "水印透明度百分比",
    "hint": "范围 0 到 100，数字越大水印越明显。默认 8。",
    "default": 8
  },
  "watermark_density": {
    "type": "string",
    "description": "水印密度",
    "hint": "low=稀疏，medium=默认，high=密集。",
    "options": [
      "low",
      "medium",
      "high"
    ],
    "default": "medium"
  },
  "github_token": {
    "type": "string",
    "description": "GitHub Personal Access Token",
    "hint": "可选。用于提高 GitHub API 访问限额。获取位置：https://github.com/settings/tokens 。公开仓库只读访问即可：Fine-grained token 选择 Public repositories read-only；Classic token 可不勾选 scopes。留空则匿名访问，限额较低。",
    "default": ""
  },
  "github_max_retry_count": {
    "type": "int",
    "description": "GitHub 请求最大重试次数",
    "hint": "默认 4。GitHub 请求遇到网络错误、限流或 5xx 临时错误时会重试，等待时间按 1、2、4、8 分钟递增；遇到权限、仓库不存在等明确错误不会重试。",
    "default": 4
  },
  "max_files_per_report": {
    "type": "int",
    "description": "每次模型请求最多文件数",
    "hint": "默认 150。设置为 0 表示不限制。若设置为大于 0，会先按文件数拆出基础模型请求，且每个请求不会超过该文件数上限；单个文件不会被拆开。",
    "default": 150
  },
  "max_input_tokens": {
    "type": "float",
    "description": "每次模型请求最大 token 输入",
    "hint": "默认 0.1M。设置为 0 表示不限制。支持最多两位小数，例如 0.25K 或 1.5M。文件数限制先应用，再按完整模型输入估算 token；超过后会在保证文件完整性的前提下继续拆分任务。",
    "default": 0.1
  },
  "max_input_token_unit": {
    "type": "string",
    "description": "token 输入单位",
    "hint": "K 表示千 token，M 表示百万 token。",
    "options": [
      "K",
      "M"
    ],
    "default": "M"
  },
  "max_retry_count": {
    "type": "int",
    "description": "最大重试次数",
    "hint": "模型请求失败后，每次把上次失败任务拆成两半再重试；只按文件边界拆分，不损坏文件完整性。默认 2。",
    "default": 2
  },
  "enable_model_tool_calls": {
    "type": "bool",
    "description": "允许模型请求补充上下文",
    "hint": "开启后，分析模型可以在 JSON 的 tool_calls 中申请读取本次 diff 的 patch、读取目标提交文件全文、搜索变更文件或列出关联文件。read_changed_file 本地没有完整文件内容时允许额外从 GitHub 拉取目标提交下的文件。",
    "default": false
  },
  "max_tool_call_rounds": {
    "type": "int",
    "description": "每个分片最多工具调用轮数",
    "hint": "每个分片分析后，模型最多可申请多少轮补充上下文。设置为 0 等同关闭工具补充分析。",
    "default": 2
  },
  "max_tool_calls_per_round": {
    "type": "int",
    "description": "每轮最多工具调用数",
    "hint": "限制模型每轮最多申请多少个工具调用，超出的请求会被拒绝并写入任务日志。",
    "default": 5
  },
  "max_tool_result_chars": {
    "type": "int",
    "description": "单次工具结果最大字符数",
    "hint": "限制每个工具返回给模型的最大字符数，避免补充上下文过大。默认 12000。",
    "default": 12000
  },
  "tool_call_prompt": {
    "type": "text",
    "description": "工具调用提示词",
    "hint": "用于指导模型什么时候可以申请补充上下文。插件会追加固定 JSON 协议说明；这里填写业务侧偏好和限制。",
    "default": "当当前分片不足以判断关联挂载、完整参数或同名配置时，可以通过 tool_calls 申请补充上下文。只请求本次 diff 涉及或强相关的文件；优先使用 read_changed_patch、read_changed_file、search_changed_files、list_related_files。不要请求无关路径，不要为了普通概括请求工具。"
  },
  "enable_dynamic_context_queue": {
    "type": "bool",
    "description": "启用动态补充请求队列",
    "hint": "默认开启。模型在 context_requests 中说明哪个文件缺少哪些文件后，插件会把源文件和缺少文件放入下一轮模型请求继续分析；如果模型只写 uncertainties 但漏写 context_requests，插件只会按不确定点中明确点名的变更文件自动生成补充请求，不再按同目录或同后缀猜测补充文件。",
    "default": true
  },
  "max_dynamic_context_rounds": {
    "type": "int",
    "description": "动态补充最大轮数",
    "hint": "每次更新最多执行多少轮 context_requests 动态补充分析。设置为 0 等同关闭动态补充队列。",
    "default": 1
  },
  "max_dynamic_context_requests": {
    "type": "int",
    "description": "动态补充请求上限",
    "hint": "每次更新最多实际入队多少个动态补充请求，超过后跳过并写入任务日志。设置为 0 表示不执行动态补充。",
    "default": 8
  },
  "max_dynamic_files_per_request": {
    "type": "int",
    "description": "每个动态请求最多补充文件数",
    "hint": "限制单个 context_requests.missing_files 最多带入多少个缺少文件，避免单次补充请求过大。",
    "default": 4
  },
  "max_dynamic_context_chars": {
    "type": "int",
    "description": "动态补充上下文字符上限",
    "hint": "限制单个动态补充请求实际带入模型的文件上下文字符数。默认 12000；设置为 0 表示不限制。超过上限的缺少文件会跳过，全部缺少文件都超限时本次动态补充请求会跳过。",
    "default": 12000
  },
  "clear_cache_files": {
    "type": "bool",
    "description": "清空缓存文件",
    "hint": "默认关闭。开启后插件下次加载时会清空插件数据目录中的 GitHub 缓存、模型分析缓存、日志、图片和状态文件，然后自动把此开关改回关闭。",
    "default": false
  },
  "terminate_running_task": {
    "type": "bool",
    "description": "终止当前正在运行的任务",
    "hint": "默认关闭。开启后，正在执行的检查任务会在阶段切换、GitHub 重试等待、模型分片、动态补充、报告生成和推送循环中检测到该开关并尽快终止；终止后不会标记本次 commit 完成，并会自动改回关闭。",
    "default": false
  },
  "enable_task_lock": {
    "type": "bool",
    "description": "任务锁",
    "hint": "默认关闭。开启后，如果已有检查任务正在运行，新的定时或手动检查会直接拒绝执行，不会排队等待；插件重载时会自动改回关闭。",
    "default": false
  },
  "max_saved_artifacts": {
    "type": "int",
    "description": "插件文件最大保存数量",
    "hint": "logs 目录按任务结果目录保留数量，images、errors、task_logs 目录按文件保留数量。默认 5，超过后删除旧项目并保留最新项目；设置为 0 表示不限制。GitHub 缓存目录 github_cache 和模型分析缓存目录 analysis_cache 不受此项限制。",
    "default": 5
  },
  "restore_default_prompts": {
    "type": "bool",
    "description": "恢复内置提示词",
    "hint": "默认关闭。开启后插件下次加载时会把分析提示词、总结提示词、工具调用提示词、监督提示词恢复为内置默认值，然后自动改回关闭。",
    "default": false
  }
}
